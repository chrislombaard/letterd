# Database Configuration (Required)
# For local PostgreSQL: postgresql://username:password@localhost:5432/database_name
# For development, you can use: postgresql://postgres:password@localhost:5432/letterd
DATABASE_URL="postgresql://postgres:password@localhost:5432/letterd"

# Database URLs (for Prisma Cloud/Accelerate - optional)
# PRISMA_DATABASE_URL="your_prisma_accelerate_url"  
# DIRECT_URL="your_direct_database_url"

# Email Configuration (Optional - uses mock email service if not configured)
# Option 1: Use Resend sandbox (free, no domain verification needed)
RESEND_API_KEY="re_your_api_key_from_resend_dashboard"
FROM_EMAIL="onboarding@resend.dev"

# Option 2: Use your own verified domain (requires Resend custom domain plan)
# RESEND_API_KEY="re_your_api_key_from_resend_dashboard" 
# FROM_EMAIL="noreply@notifications.yourdomain.com"

# Admin Authentication (Required for /admin routes)
# Change these to secure values in production
ADMIN_USER="admin"
ADMIN_PASS="secure_admin_password_123"

# Cron Security (Required for scheduled email processing)
# Generate a random secret: openssl rand -hex 32
CRON_SECRET="d3066c770507f8ab934969b58dd195b11da9765bc8c9db6817bf7f38f7533c2c"

# App URL (Optional - used for unsubscribe links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Development Notes:
# - Without RESEND_API_KEY, emails are logged to console (for testing)
# - Use the setup script: node scripts/setup-email.js for guided configuration
# - For quick start: just set DATABASE_URL and ADMIN_PASS, everything else is optional